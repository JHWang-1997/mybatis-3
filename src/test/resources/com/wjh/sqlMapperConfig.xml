<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
  <properties resource="com/wjh/jdbc.properties">
  </properties>

  <settings>
    <setting name="lazyLoadingEnabled" value="true"/>
  </settings>


  <typeAliases>
    <package name="com.wjh.pojo"/>
  </typeAliases>

  <environments default="default">
    <environment id="default">
      <!--JDBC是"class org.apache.ibatis.transaction.jdbc.JdbcTransactionFactory"的内置别名-->
      <transactionManager type="JDBC"></transactionManager>
      <!--POOLED是 "class org.apache.ibatis.datasource.pooled.PooledDataSourceFactory"的内置别名-->
      <dataSource type="POOLED">
        <property name="driver" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
      </dataSource>
    </environment>
  </environments>


  <mappers>
    <package name="com.wjh.mapper"/>
  </mappers>


</configuration>
